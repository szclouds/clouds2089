package com.clouds.designPattern.observer.springEvent;

/**
 * @author clouds
 * @version 1.0
 */
public class CustomMsgChild extends CustomMsg {
    CustomMsgChild(String fieldName, int total) {
        super(fieldName, total);
    }
}
